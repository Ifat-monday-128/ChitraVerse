# Project instructions

## Steps to follow

1. activate all buttons logically and make necessary endpoints.
2. make sure search button works genuinely following standard search algorithm followed by world's top sites.
3. currently, the database stores all treailer links from  tmdb, replace them by using web search and store the youtube link in the form of abc where the link is youtube.com/abc
4. only hollywood films should be displayed in the home page.
5. currently the intersteller poster is being shown a s a svg manufactured(not in database). delete these unnecessary files. and make sure the web never shows anything outside the database

## Rules and constraints

1. dont change our database structure too much unless its one time and really necessary.

## Checks before finishing

always make sure that the server and frontend runs properly befotre finishing your work.
